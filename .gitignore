# Go toolchain caches (managed by the OpenCode dev environment)
.cache/
.tmp/

# Relay default state file (contains sensitive location data)
state.json

# Compose secrets (never commit tokens)
deploy/.env