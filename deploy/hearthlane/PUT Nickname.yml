info:
  name: PUT Nickname
  type: http

http:
  method: PUT
  url: http://localhost:8080/devices/hearthlance-device-1/nickname
  body:
    type: json
    data: '{"nickname":"Device 1"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
